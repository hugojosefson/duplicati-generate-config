{
  "name": "duplicati-generate-config",
  "version": "1.0.0",
  "description": "Generate duplicati backup configs from a flat file",
  "main": "src/api-commonjs.js",
  "module": "src/api.js",
  "files": [
    "bin",
    "src"
  ],
  "bin": {
    "duplicati-generate-config": "bin/duplicati-generate-config.js"
  },
  "scripts": {
    "start": "bin/duplicati-generate-config.js",
    "test": "run-s lint mocha",
    "mocha": "mocha --require @std/esm",
    "lint": "standard",
    "lint-fix": "standard --fix",
    "prepare": "run-s documentation",
    "documentation": "documentation readme --section=API --shallow --format=md src/api.js",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.{mjs,js,jsx}": [
      "standard --fix",
      "git add"
    ]
  },
  "keywords": [
    "duplicati",
    "config",
    "backup"
  ],
  "author": "Hugo Josefson <hugo@josefson.org> (https://www.hugojosefson.com/)",
  "license": "ISC",
  "comment-engines.node": "Not using babel, code seems to work from v8.3.0 and up.",
  "engines": {
    "node": ">=8.3.0"
  },
  "comment-@std/esm.esm": "tap only saw test files named .js, but what about mocha? can we rename them back to .mjs and stay in line with Node.js compat?",
  "comment-@std/esm.cjs": "Allow destructuring import from common-js modules.",
  "@std/esm": {
    "esm": "all",
    "cjs": true
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "documentation": "^5.3.2",
    "husky": "^0.14.3",
    "lint-staged": "^4.2.3",
    "mocha": "^3.5.3",
    "npm-run-all": "^4.1.1",
    "standard": "^10.0.3"
  },
  "dependencies": {
    "@std/esm": "^0.11.0",
    "engine-check": "^1.0.0"
  }
}
